# SGLang Weekly Changelog — 2026-08-29

Diff range: `0db2c53deca12715d209ffbd46bdfafdfd5a5d35..46ccd7ce3e70455a971e6a7f7765cd78bc246322`

## 新增参数

| 前一个参数/新参数分组 | 参数名称 | 默认值 | 可选值 | 描述 |
|---|---|---|---|---|
| --dist-init-addr | --gated-launch-port | None | Type: Optional[int] | The port of the gated launch control server. When set, every rank blocks right after the distributed environment is initialized, before any sizable GPU allocation, until `POST /gate/activate` is sent to this port on the host of the first rank. This lets an external orchestrator defer the memory hungry part of startup to a safe window. Defaults to None, which disables the gate. |
| --enable-attn-tp-input-scattered | --enable-shared-experts-attn-tp | False | bool flag (set to enable) | Shard shared expert weights across the attention TP group when using an expert-parallel all-to-all backend. |
| --enable-shared-experts-attn-tp | --enable-dense-mlp-attn-tp | False | bool flag (set to enable) | Shard dense MLP weights across the attention TP group under DP attention. |
| --kv-events-config | --load-publish-endpoint | None | Type: Optional[str] | Opt in to the runtime-load PUB socket that load-aware routers subscribe to. Off by default (unset or 'off'). Use 'auto' to reserve the dp_size ports packed after the --kv-events-config range, or a wildcard-host TCP address (e.g. tcp://*:6000) to place it explicitly; rank r binds port+r and /server_info advertises the base under the kv_events block. Requires --kv-events-config to describe a publisher (routers discover the base through /server_info); startup fails if this is set without one, is not bindable, or overlaps the KV range. Note: 'auto' reserves 2*dp_size ports from the KV base — space co-hosted engines accordingly. The router-facing update cadence follows --load-snapshot-publish-interval (shared to avoid double-collecting the snapshot), so a large value there also staleness-caps this feed. |
| --decode-attention-backend | --enable-lean-attention | None | bool flag (set to enable) | Enable Lean (Work-Centric) Attention decode kernel for long-context serving. When None (default), uses auto-gate that activates Lean for long contexts and falls back to standard kernel for short contexts. Set to True to force enable, False to force disable. |
| --speculative-draft-attention-backend | --speculative-dsa-topk-backend | sgl-kernel | sgl-kernel, torch, flashinfer | DSA indexer top-k backend for speculative draft workers. Options: 'sgl-kernel', 'torch', 'flashinfer'. The 'torch' backend currently requires SGLANG_DSA_FUSE_TOPK=false. |
| --hicache-storage-backend-extra-config | --hicache-storage-prefetch-retry-poll-interval | 0 | Type: int | Scheduling passes a queued request waits after a storage prefetch miss before the availability check is retried (under load the first check can run before the needed backup commits). 0 disables retries. |
| --hicache-storage-prefetch-retry-poll-interval | --hicache-storage-prefetch-retry-max-attempts | 4 | Type: int | Maximum storage prefetch retries per request when --hicache-storage-prefetch-retry-poll-interval is set. |

## 新增模型

| 模型类型 | 模型族 | 模型名称 |
|---|---|---|
| Large Language Model | Granite SWA | ibm-granite/granite-swash-2b |
| Large Language Model | Granite MoE SWA | ibm-granite/granite-swash-3b-a600m |
| Large Language Model | Granite 4.2 | ibm-granite/granite-4.2-3b, ibm-granite/granite-4.2-8b, ibm-granite/granite-4.2-30b |
| Large Language Model | GLM-5.3 | zai-org/GLM-5.3, zai-org/GLM-5.3-BF16, RadixArk/GLM-5.3-NVFP4 |
| Multimodal Language Model | GLM-5.3-Flash | zai-org/GLM-5.3-Flash |
| Large Language Model | Qwen3.8-Flash-Next | Qwen/Qwen3.8-Flash-Next, Qwen/Qwen3.8-Flash-Next-FP8, RadixArk/Qwen3.8-Flash-Next-NVFP4 |
| Large Language Model | Hy4 preview | tencent/Hy4-preview, tencent/Hy4-preview-FP8 |
| Diffusion Image Model | LongCat-Image | meituan-longcat/LongCat-Image |
