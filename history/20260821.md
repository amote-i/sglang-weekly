# SGLang Weekly Changelog — 2026-08-21

Diff range: `18107e38d266a98f59a8e3c766f8ddaf9b723ded..0db2c53deca12715d209ffbd46bdfafdfd5a5d35`

## 新增参数

| 前一个参数/新参数分组 | 参数名称 | 默认值 | 可选值 | 描述 |
|---|---|---|---|---|
| --chunked-prefill-size | --prefill-decode-interval | 0 | Type: int | The number of decode rounds to run after a prefill batch before scheduling the next prefill. In data-parallel attention mode, the interval is synchronized across all DP ranks. Set to 0 to disable. |
| --page-size | --c128-page-size | 16 | Type: int | The physical page size of the NPU DSV4 C128 KV cache. Must be a positive multiple of 16. |
| --enable-dp-lm-head | --enable-tp-lm-head-all-to-all | None (set in __post_init__, initial value: None) | Type: bool (BooleanOptionalAction, --no- prefix supported) | Use all-to-all instead of TP all-gather followed by DP scatter for the TP-sharded LM head under DP attention. By default this is enabled only on decode-only PD nodes with pure DP attention (tp_size == dp_size > 1 and attn_cp_size == 1), and disabled on prefill-only and colocated nodes. Pass --no-enable-tp-lm-head-all-to-all to opt out. The path is incompatible with --enable-dp-lm-head; batches without an equal padded row count fall back to the existing all-gather path. |
| --enable-http2 | --http2-max-concurrent-streams | 200 | Type: int | Maximum number of concurrent streams advertised on each HTTP/2 connection (1 to 2^32 - 1). Only applies with --enable-http2. |
| --dsa-prefill-backend | --dsv4-prefill-backend | auto | auto, flashmla_sparse, flashmla_sparse_q8 | DeepSeek-V4 sparse prefill backend. 'auto' and 'flashmla_sparse' use the existing BF16 sparse prefill path; 'flashmla_sparse_q8' enables the Q8KV8 sparse prefill path. |
| --expert-distribution-recorder-buffer-size | --expert-balancedness-report-mode | off | off, server_log, prometheus, both | Where to report expert balancedness. Options: off, server_log, prometheus, both. |
| --enable-hierarchical-cache | --hicache-host-memory-mode | cache | cache, buffer_only | Whether host memory is a persistent HiCache tier (cache) or a transient staging buffer between GPU and the storage backend (buffer_only). buffer_only requires --hicache-storage-backend. |
| --mm-io-worker-num | --allowed-media-domains | [] (computed at runtime: dataclasses.field(default_factory=list)) | Type: List[str] | Restrict client-supplied HTTP(S) image, video, and audio URLs to these exact hostnames. Redirect destinations are checked against the same allowlist. When unset, remote media from any domain is allowed. |
| --allowed-media-domains | --media-url-max-file-size-mb | 64 | Type: int | Maximum size in MiB for one client-supplied remote media download. The limit is enforced while streaming; set to 0 to disable it. |
| --disaggregation-decode-enable-offload-kvcache | --disaggregation-decode-retraction-backup | None | cpu_tensor, host_pool | Storage backend for KV preserved across PD decode retraction. 'cpu_tensor' uses per-request CPU tensors. 'host_pool' uses a reserved HiCache pool and does not fall back on exhaustion. If omitted, the backend is inferred from the decode KV pool. |

## 新增模型

| 模型类型 | 模型族 | 模型名称 |
|---|---|---|
| Multimodal Language Model | PaddleOCR-VL (0.9B, 1.5, 1.6) | PaddlePaddle/PaddleOCR-VL-1.6 |
